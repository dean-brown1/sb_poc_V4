# configs/baseline_code_contests_2epochs.yaml

experiment:
  name: "baseline_code_contests_2epochs"
  seed: 42
  mode: "baseline"
  output_dir: "./results/baseline_code_contests_2epochs"

model:
  base_model: "Qwen/Qwen2-0.5B"
  torch_dtype: "bfloat16"
  device_map: "auto"

lora:
  r: 8
  lora_alpha: 16
  lora_dropout: 0.05
  target_modules: ["q_proj", "v_proj", "k_proj", "o_proj"]
  bias: "none"
  task_type: "CAUSAL_LM"

training:
  total_steps: 3000  # 2000 examples * 6 epochs / 4 grad_accum = 3000 steps
  batch_size: 1
  gradient_accumulation_steps: 4
  seq_len: 1024  # Code can be longer than GSM8K
  learning_rate: 0.0001
  weight_decay: 0.01
  warmup_ratio: 0.05
  max_grad_norm: 1.0

dataset:
  name: "code_contests"
  split_train: "train"
  split_test: "test"
  num_train_samples: 2000  # Subsample to 2000 examples
  
evaluation:
  eval_interval: 100
  num_test_samples: 50  # Test set only has 165 examples
  max_new_tokens: 512  # Code generation needs more tokens
  do_sample: false
  
logging:
  log_every_n_steps: 1
  save_final_only: true